server:
  port: 8080  
spring:
    application:
      name: DEPARTMENT-SERVICE
    datasource:
     password: postgres
     url: jdbc:postgresql://localhost:5432/department?currentSchema=test
     username: postgres
    jpa:
     hibernate:
      ddl-auto: update
     properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
     show-sql: true
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
message=Hello, Kousik
